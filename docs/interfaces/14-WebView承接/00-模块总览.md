---
tags:
  - interface
  - api
aliases:
  - WebView 承接入口
---

# WebView 承接入口

返回 [[00-接口总览]]。

## 本模块接口

- [[14-WebView承接/01-客户中心|客户中心]]：App 内 WebView 承接入口：客户中心。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/02-绑定客户编码|绑定客户编码]]：App 内 WebView 承接入口：绑定客户编码。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/03-月结中心|月结中心]]：App 内 WebView 承接入口：月结中心。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/04-号码保护|号码保护]]：App 内 WebView 承接入口：号码保护。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/05-隐私政策|隐私政策]]：App 内 WebView 承接入口：隐私政策。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/06-个人信息清单|个人信息清单]]：App 内 WebView 承接入口：个人信息清单。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/07-电子运单服务协议|电子运单服务协议]]：App 内 WebView 承接入口：电子运单服务协议。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/08-合作方清单|合作方清单]]：App 内 WebView 承接入口：合作方清单。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/09-权限调用清单|权限调用清单]]：App 内 WebView 承接入口：权限调用清单。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/10-已签署免赔协议|已签署免赔协议]]：App 内 WebView 承接入口：已签署免赔协议。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/11-服务协议|服务协议]]：App 内 WebView 承接入口：服务协议。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/12-服务协议|服务协议]]：App 内 WebView 承接入口：服务协议。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/13-隐私政策|隐私政策]]：App 内 WebView 承接入口：隐私政策。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/14-隐私政策|隐私政策]]：App 内 WebView 承接入口：隐私政策。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/15-订单投诉|订单投诉]]：App 内 WebView 承接入口：订单投诉。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/16-在线理赔|在线理赔]]：App 内 WebView 承接入口：在线理赔。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/17-修改运单|修改运单]]：App 内 WebView 承接入口：修改运单。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/18-催单进度|催单进度]]：App 内 WebView 承接入口：催单进度。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/19-服务评价|服务评价]]：App 内 WebView 承接入口：服务评价。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/20-收件方式|收件方式]]：App 内 WebView 承接入口：收件方式。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/21-电子合同|电子合同]]：App 内 WebView 承接入口：电子合同。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/22-投诉|投诉]]：App 内 WebView 承接入口：投诉。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
- [[14-WebView承接/23-在线理赔|在线理赔]]：App 内 WebView 承接入口：在线理赔。这不是 depponHttp JSON 接口，而是 H5 页面或文件预览入口。
